<UserControl x:Class="HDSInspector.InspectionResultNGNameList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Border Grid.Row="0" Grid.RowSpan="2" Grid.Column="0" BorderBrush="LightGray" BorderThickness="1"/>

        <ScrollViewer Name="NGContListScrollViewer" Grid.Row = "1" Grid.Column="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" CanContentScroll="True" Background="Transparent">
            <ListBox x:Name="lbNGCheckBoxList" IsSynchronizedWithCurrentItem="True" SelectionMode="Single" Background="Transparent">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height ="25"/>
                            </Grid.RowDefinitions>
                            <StackPanel Width="96" Orientation="Horizontal" HorizontalAlignment="Center" Margin="17,0,0,0">
                                <RadioButton VerticalAlignment="Center" GroupName="ngGroup" Margin="0,0,0,0" IsChecked="{Binding Path=IsNGChecked}"  x:Name="chkNGChecked"/>
                                <TextBlock HorizontalAlignment="Left" FontSize="15"  VerticalAlignment="Center" x:Name="txtNGName1" Text="{Binding Path=NGName1}" TextAlignment="Center" Margin="3,0,0,0"/>
                            </StackPanel>                            
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>
    </Grid>
</UserControl>

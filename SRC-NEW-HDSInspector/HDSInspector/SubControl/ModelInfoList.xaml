<UserControl x:Class="HDSInspector.SubControl.ModelInfoList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="500">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="5"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <!-- No. | Group -->
        <StackPanel Grid.Row="0" Grid.Column="1" Margin="0, 2, 0, 0" Orientation="Horizontal">
            <StackPanel.Effect>
                <DropShadowEffect ShadowDepth="5" Direction="310" Color="DarkGray" BlurRadius="4" Opacity="0.5"/>
            </StackPanel.Effect>
            <StackPanel.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="#FF919191" Offset="1"/>
                    <GradientStop Color="White"/>
                </LinearGradientBrush>
            </StackPanel.Background>
            <TextBlock Text="Group" VerticalAlignment="Center" FontSize="12" Margin="10, 0, 0, 0"/>
        </StackPanel>
        <Border Grid.Row="0" Grid.ColumnSpan="1" Grid.Column="1" BorderBrush="#FF919191" BorderThickness="1" Margin="0, 2, 0, 0"></Border>
        <!-- listbox -->
        <ScrollViewer x:Name="svGroup" Background="White"
                Grid.Row="1" Grid.ColumnSpan="1" Grid.Column="1" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Visible">
            <ScrollViewer.Effect>
                <DropShadowEffect ShadowDepth="5" Direction="310" Color="DarkGray" BlurRadius="4" Opacity="0.5"/>
            </ScrollViewer.Effect>
            <ListBox x:Name="lbGroup" ItemsSource="{Binding}" IsSynchronizedWithCurrentItem="True">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Group}"  Margin="15, 0, 0, 0"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>

        <!-- No. | Model -->
        <StackPanel Grid.Row="0" Grid.Column="3" Grid.ColumnSpan="1" Margin="0, 2, 0, 0" Orientation="Horizontal">
            <StackPanel.Effect>
                <DropShadowEffect ShadowDepth="5" Direction="310" Color="DarkGray" BlurRadius="4" Opacity="0.5"/>
            </StackPanel.Effect>
            <StackPanel.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="#FF919191" Offset="1"/>
                    <GradientStop Color="White"/>
                </LinearGradientBrush>
            </StackPanel.Background>
            <TextBlock Text="Model" VerticalAlignment="Center" FontSize="12" Margin="10, 0, 0, 0"/>
            
        </StackPanel>
        <Border Grid.Row="0" Grid.Column="3" Grid.ColumnSpan="1" BorderBrush="#FF919191" BorderThickness="1" Margin="0, 2, 0, 0"/>
        <Button Grid.Column="3" Width="23" Margin="2" BorderBrush="#FF919191" HorizontalAlignment="Right" Name="btnClose">X</Button>

        <!-- listbox -->
        <ScrollViewer x:Name="svModel" Background="White"
                Grid.Row="1" Grid.Column="3" Grid.ColumnSpan="1" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Visible">
            <ScrollViewer.Effect>
                <DropShadowEffect ShadowDepth="5" Direction="310" Color="DarkGray" BlurRadius="4" Opacity="0.5"/>
            </ScrollViewer.Effect>
            <ListBox x:Name="lbModel" ItemsSource="{Binding}" IsSynchronizedWithCurrentItem="True">
               
            </ListBox>
        </ScrollViewer>
    </Grid>
</UserControl>

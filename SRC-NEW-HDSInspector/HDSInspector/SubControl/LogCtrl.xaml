<UserControl x:Class="HDSInspector.LogCtrl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Border BorderBrush="#FF919191" BorderThickness="1">
            <Border.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="#FF919191" Offset="1"/>
                    <GradientStop Color="White"/>
                </LinearGradientBrush>
            </Border.Background>
            <StackPanel Orientation="Horizontal">
                <TextBlock FontSize="12" FontFamily="Arial"
                           Width="110" VerticalAlignment="Center" Margin="40, 0, 0, 0">Time</TextBlock>
                <TextBlock Grid.Row="0" Grid.Column="1" FontSize="12" FontFamily="Arial"
                           VerticalAlignment="Center" Margin="60, 0, 0, 0">Message</TextBlock>
            </StackPanel>
        </Border>

        <Border Grid.Row="1">
            <Border.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0" Opacity="0.5">
                    <GradientStop Color="#77444444" Offset="1"/>
                    <GradientStop Color="White" Offset="0.1"/>
                </LinearGradientBrush>
            </Border.Background>                
        </Border>
        <TextBlock Grid.Row="1" FontWeight="Bold" Margin="20,0,0,0" Foreground="#E9E9E9" HorizontalAlignment="Center" VerticalAlignment="Center"  FontFamily="Arial" FontSize="70">Haesung DS</TextBlock>
        <ScrollViewer HorizontalScrollBarVisibility="Hidden" Name="svLogViewer" VerticalScrollBarVisibility="Auto" Grid.Row="1"
                      HorizontalAlignment="Stretch">
            <ListBox SelectionMode="Single" Name="lbLog" Background="Transparent">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Path=Time, StringFormat='MM/dd hh:mm tt'}" Width="110" Margin="5, 0, 0, 0"/>
                            <TextBlock Text="{Binding Path=Message}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>
        
    </Grid>
</UserControl>

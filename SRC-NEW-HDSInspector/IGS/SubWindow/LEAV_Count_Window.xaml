<Window x:Class="IGS.SubWindow.LEAV_Count_Window"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IGS.SubWindow"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="퇴근 수량 입력" Height="400" Width="870" WindowStyle="None" WindowStartupLocation="CenterScreen">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Background="Black" BorderBrush="Gray" BorderThickness="1" Name="bdTitle">
            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,0,0" Foreground="White" FontSize="20" FontWeight="Bold" Text="퇴근 수량 입력"/>
        </Border>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="100" Margin="2,4" BorderThickness="0" Background="DimGray" Cursor="Hand" Name="btnCancel">
                <Button.Resources>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold" Foreground="White" FontSize="20" Text="취 소"/>
            </Button>
            <Button Width="100" Margin="2,4" BorderThickness="0" Background="OrangeRed" Cursor="Hand" Name="btnOK">
                <Button.Resources>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold" Foreground="White" FontSize="20" Text="확 인"/>
            </Button>
        </StackPanel>
        
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="15"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderThickness="1" BorderBrush="Silver" Background="#FFC2CADC">
                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="18" FontWeight="Bold" Text="설비 코드"/>
            </Border>
            <Border Grid.Column="1" BorderThickness="1" BorderBrush="Silver" Background="#FFC2CADC">
                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="18" FontWeight="Bold" Text="퇴근 투입 수량"/>
            </Border>
            <Border Grid.Column="2" BorderThickness="1" BorderBrush="Silver" Background="#FFC2CADC">
                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="18" FontWeight="Bold" Text="퇴근 양품 수량"/>
            </Border>
            <Border Grid.Column="3" BorderThickness="1" BorderBrush="Silver" Background="#FFC2CADC">
                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="18" FontWeight="Bold" Text="퇴근 불량 수량"/>
            </Border>
            <Border Grid.Column="4" BorderThickness="1" BorderBrush="Silver" Background="#FFC2CADC">
                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="18" FontWeight="Bold" Text="유실 코드"/>
            </Border>
            <Border Grid.Column="5" BorderThickness="1" BorderBrush="Silver" Background="#FFC2CADC"/>
        </Grid>

        <ScrollViewer Grid.Row="2" x:Name="svInfo" Background="Transparent" VerticalScrollBarVisibility="Visible" 
                      HorizontalScrollBarVisibility="Hidden" ScrollViewer.CanContentScroll="True">
            <ItemsControl>
                <ListBox x:Name="lbInfo" ItemsSource="{Binding}" IsSynchronizedWithCurrentItem="True" AlternationCount="2">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Style.Triggers>
                                <Trigger Property="ItemsControl.AlternationIndex" Value="0">
                                    <Setter Property="Background" Value="White"/>
                                </Trigger>
                                <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                                    <Setter Property="Background" Value="WhiteSmoke"/>
                                </Trigger>
                            </Style.Triggers>
                            <!--<Setter Property="Focusable" Value="False"/>-->
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Height="35" Margin="-5,0,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="110"/>
                                    <ColumnDefinition Width="145"/>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="{Binding mc_code}" Margin="0,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold" FontSize="15"/>
                                <TextBox Grid.Column="1" Text="{Binding InputCount, StringFormat=\{0:N0\}, UpdateSourceTrigger=PropertyChanged}" Margin="15,3" VerticalContentAlignment="Center" HorizontalContentAlignment="Right" FontSize="16"
                                         PreviewMouseLeftButtonUp="TextBox_PreviewMouseLeftButtonUp"/>
                                <TextBox Grid.Column="2" Text="{Binding GoodCount, StringFormat=\{0:N0\}, UpdateSourceTrigger=PropertyChanged}" Margin="15,3" VerticalContentAlignment="Center" HorizontalContentAlignment="Right" FontSize="16" IsReadOnly="True"/>
                                <StackPanel Grid.Column="3" Orientation="Horizontal" Width="190">
                                    <TextBox Width="130" Text="{Binding NGTotalCount, StringFormat=\{0:N0\}, UpdateSourceTrigger=PropertyChanged}" Margin="5,3" VerticalContentAlignment="Center" HorizontalContentAlignment="Right" FontSize="16" IsReadOnly="True"/>
                                    <Button Width="35" Margin="3" Background="#FF102239" BorderThickness="0" Cursor="Hand" Tag="{Binding mc_code}" x:Name="btnNG" Click="btnNG_Click">
                                        <Button.Resources>
                                            <Style TargetType="{x:Type Border}">
                                                <Setter Property="CornerRadius" Value="5"/>
                                            </Style>
                                        </Button.Resources>
                                        <Image Source="../Images/search_white.png" Width="20" Height="20"/>
                                    </Button>
                                </StackPanel>
                                <StackPanel Grid.Column="4" Orientation="Horizontal" Width="235" Margin="5,0,0,0">
                                    <ComboBox Margin="2,3" Width="230" MaxDropDownHeight="350" VerticalContentAlignment="Center" Name="cbLossCode" Text="{Binding lossCode}"
                                          ItemsSource="{Binding lossItems}" MouseEnter="cbLossCode_MouseEnter" MouseLeave="cbLossCode_MouseLeave"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>

<Window x:Class="IGS.SubWindow.VERIFY_USER_LIST_Window"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IGS.SubWindow"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        Title="VERIFY 작업자 선택" Height="350" Width="350">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="Black" BorderBrush="Gray" BorderThickness="1" Name="bdTitle">
            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,0,0" Foreground="White" FontSize="20" FontWeight="Bold" Text="VERIFY 작업자 선택"/>
        </Border>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="70" Margin="2,4" BorderThickness="0" Background="DimGray" Cursor="Hand" Name="btnCancel">
                <Button.Resources>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold" Foreground="White" FontSize="20" Text="취소"/>
            </Button>
            <Button Width="70" Margin="2,4" BorderThickness="0" Background="OrangeRed" Cursor="Hand" Name="btnOK">
                <Button.Resources>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold" Foreground="White" FontSize="20" Text="확인"/>
            </Button>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition />
                <ColumnDefinition Width="70"/>
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderThickness="1" BorderBrush="Silver" Background="SkyBlue">
                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="17" FontWeight="Bold" Text="사번"/>
            </Border>
            <Border Grid.Column="2" BorderThickness="1" BorderBrush="Silver" Background="SkyBlue">
                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="17" FontWeight="Bold" Text="이름"/>
            </Border>

            <TextBox Grid.Column="1" Margin="5,3" VerticalContentAlignment="Center" FontSize="17" FontWeight="Bold" Name="tboxID">
                <TextBox.Resources>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </TextBox.Resources>
            </TextBox>
            <TextBox Grid.Column="3" Margin="5,3" VerticalContentAlignment="Center" FontSize="17" FontWeight="Bold" Name="tboxName">
                <TextBox.Resources>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </TextBox.Resources>
            </TextBox>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderThickness="1" BorderBrush="Silver" Background="#FFC2CADC"/>
            <Border Grid.Column="1" BorderThickness="1" BorderBrush="Silver" Background="#FFC2CADC">
                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="17" FontWeight="Bold" Text="사번"/>
            </Border>
            <Border Grid.Column="2" BorderThickness="1" BorderBrush="Silver" Background="#FFC2CADC">
                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="17" FontWeight="Bold" Text="이름"/>
            </Border>
        </Grid>

        <Border Grid.Row="3" BorderThickness="1" BorderBrush="Silver"/>
        <ScrollViewer Grid.Row="3" x:Name="svInfo" Background="Transparent" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Hidden" ScrollViewer.CanContentScroll="True" Margin="1">
            <ListView x:Name="lbInfo" ItemsSource="{Binding}" IsSynchronizedWithCurrentItem="True" AlternationCount="2">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Style.Triggers>
                            <Trigger Property="ItemsControl.AlternationIndex" Value="0">
                                <Setter Property="Background" Value="White"/>
                            </Trigger>
                            <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                                <Setter Property="Background" Value="WhiteSmoke"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Height="30" Margin="10,0,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="320"/>
                            </Grid.ColumnDefinitions>

                            <CheckBox Grid.Column="0" Height="30" IsChecked="{Binding bChecked}" VerticalContentAlignment="Center" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <StackPanel Orientation="Horizontal">
                                    <Border Width="150" HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding userID}" Margin="5,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16"/>
                                    </Border>
                                    <Border Width="140" HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding userName}" Margin="10,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16"/>
                                    </Border>
                                </StackPanel>
                            </CheckBox>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListView>
        </ScrollViewer>
        
    </Grid>
</Window>

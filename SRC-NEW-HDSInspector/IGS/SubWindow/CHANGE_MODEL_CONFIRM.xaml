<Window x:Class="IGS.SubWindow.CHANGE_MODEL_CONFIRM"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IGS.SubWindow"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        Title="기종교체 확인창" Height="190" Width="700">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition />
            <RowDefinition Height="30"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Grid.Column="0" Background="Black" BorderBrush="Gray" BorderThickness="1" Name="bdTitle">
            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,0,0" Foreground="White" FontSize="20" FontWeight="Bold" Text="기종교체 확인"/>
        </Border>

        <Border Grid.Row="1" Grid.RowSpan="2" BorderBrush="Silver" BorderThickness="1"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Image Source="../Images/notice.png" Width="45" Height="45" Margin="20,0,0,0"/>
            <TextBlock VerticalAlignment="Center" FontWeight="Bold" FontSize="20" Margin="10,0,0,0" Text="모델이 변경되었습니다. 기종 교체 작업을 진행하였습니까?"/>
        </StackPanel>

        <TextBlock Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Tomato" FontWeight="Bold" FontSize="13"
                   Text="* '예' 선택 시, 이전 로트 완료 시점부터 현재 시점 사이 유실이 기종 교체 손실로 처리됩니다."/>

        <Border Grid.Row="3" BorderBrush="Silver" BorderThickness="1"/>
        <Grid Grid.Row="3" Background="WhiteSmoke" Margin="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Width="150" Height="45" Background="#FF102239" BorderThickness="0" Cursor="Hand" Name="btnOK">
                <Button.Resources>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold" Foreground="White" FontSize="16" Text="예"/>
            </Button>

            <Button Grid.Column="1" Width="150" Height="45" Background="#FF102239" BorderThickness="0" Cursor="Hand" Name="btnNo">
                <Button.Resources>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold" Foreground="White" FontSize="16" Text="아니요"/>
            </Button>
        </Grid>
    </Grid>
</Window>

<Window x:Class="IGS.SubWindow.SUSPEND_CONFIRM_Window"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IGS.SubWindow"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        Title="중단보고 확인창" Height="170" Width="700">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition />
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Grid.Column="0" Background="Black" BorderBrush="Gray" BorderThickness="1" Name="bdTitle">
            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,0,0" Foreground="White" FontSize="20" FontWeight="Bold" Text="중단보고 확인"/>
        </Border>

        <Border Grid.Row="1" BorderBrush="Silver" BorderThickness="1"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Image Source="../Images/notice.png" Width="45" Height="45" Margin="20,0,0,0"/>
            <TextBlock VerticalAlignment="Center" FontWeight="Bold" FontSize="20" Margin="10,0,0,0" Text="중단보고 이후 해당설비에 다른 모델(기종교체)로 변경하십니까?"/>
        </StackPanel>

        <Border Grid.Row="2" BorderBrush="Silver" BorderThickness="1"/>
        <Grid Grid.Row="2" Background="WhiteSmoke" Margin="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Width="170" Height="45" Background="#FF102239" BorderThickness="0" Cursor="Hand" Name="btnSame">
                <Button.Resources>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold" Foreground="White" FontSize="16" Text="유지한다 (동일모델)"/>
            </Button>

            <Button Grid.Column="1" Width="170" Height="45" Background="#FF102239" BorderThickness="0" Cursor="Hand" Name="btnChange">
                <Button.Resources>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold" Foreground="White" FontSize="16" Text="변경한다 (다른모델)"/>
            </Button>

            <Button Grid.Column="2" Width="170" Height="45" Background="Tomato" BorderThickness="0" Cursor="Hand" Name="btnCancel">
                <Button.Resources>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold" Foreground="White" FontSize="16" Text="보고 취소"/>
            </Button>
        </Grid>
    </Grid>
</Window>

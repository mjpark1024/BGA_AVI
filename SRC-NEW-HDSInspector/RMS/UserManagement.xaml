<UserControl x:Class="ResultManagement.UserManagement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1024">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="50"/>
            <RowDefinition/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" HorizontalAlignment="Center" Width="600" Margin="70,0,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Grid.ColumnSpan="4" Height="45" BorderBrush="#7f7f7f" BorderThickness="8"/>
            <TextBox x:Name="txtSearch" Grid.Column="0" Grid.ColumnSpan="3" Height="30" Margin="8,8,0,8" TextWrapping="Wrap" Text="전체 검색" Background="LightGray" FontSize="16" FontFamily="Arial" FontWeight="Bold" Foreground="Gray"/>
            <ComboBox x:Name="cmSearchOption" Grid.Column="3" Height="30" Background="LightGray" BorderBrush="LightGray" FontFamily="Arial" FontSize="16" FontWeight="Bold" Margin="0,9,8,10"/>
            <Button  x:Name="btnSearch" Grid.Column="4" Height="55" Width="100" Style="{StaticResource NullBackgroundButtonStyle}" HorizontalAlignment="Left">
                <Image Source="./Images/blueButton.png" Stretch="None" Margin="0,-5,0,0"/>
            </Button>
        </Grid>

        <StackPanel Grid.Row="2" HorizontalAlignment="Right" VerticalAlignment="Bottom" Orientation="Horizontal" Margin="0,0,20,0">
            <Button x:Name="btnNew"    Height="30" Width="80" Margin="2, 0" Style="{DynamicResource SimpleButton}">등 록</Button>
            <Button x:Name="btnModify" Height="30" Width="80" Margin="2, 0" Style="{DynamicResource SimpleButton}">수 정</Button>
            <Button x:Name="btnDelete" Height="30" Width="80" Margin="2, 0" Style="{DynamicResource SimpleButton}">삭 제</Button>
        </StackPanel>
        <DataGrid Grid.Row="3" AutoGenerateColumns="False" Margin="20,10,20,10" IsReadOnly="True" Background="LightGray"
                  Name="dgUserTable">
            <DataGrid.Columns>
                <DataGridTextColumn MinWidth="120" Binding="{Binding Name}"             Header="Name"/>
                <DataGridTextColumn MinWidth="120" Binding="{Binding ID}"               Header="ID"/>
                <DataGridTextColumn MinWidth="120" Binding="{Binding Authority.AuthName}" Header="Authority"/>
                <DataGridTextColumn MinWidth="180" Binding="{Binding RegistrationDate}" Header="Registration Date"/>
                <DataGridTextColumn MinWidth="320" Binding="{Binding Password}"         Header="Password"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button x:Name="btnShowAll" Grid.Row="4" VerticalAlignment="Top" HorizontalAlignment="Right" Height="30" Width="80" Margin="5,0,20,0" Style="{DynamicResource SimpleButton}">전체보기</Button>
    </Grid>
</UserControl>

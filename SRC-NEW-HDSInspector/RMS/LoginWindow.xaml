<Window x:Class="RMS.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LoginWindow" Height="600" Width="450"
        Icon="Images/main_icon.ico"
        WindowStartupLocation="CenterScreen" WindowStyle ="None" MinHeight="600" MinWidth="450" MaxHeight="600" MaxWidth="450">
    <!--AllowsTransparency ="True" Background ="Transparent">-->
    <Window.Resources>
        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Vertical">
                    <Setter Property="Width" Value="18"/>
                    <Setter Property="Height" Value="Auto" />
                    <Setter Property="Template" Value="{StaticResource VerticalScrollBarWhite}" />
                </Trigger>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto"/>
                    <Setter Property="Height" Value="18" />
                    <Setter Property="Template" Value="{StaticResource HorizontalScrollBar}" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontFamily" Value="Arial"/>
        </Style>        
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.5*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="0.5*"/>
        </Grid.ColumnDefinitions>

        <Grid.Background>
            <ImageBrush ImageSource ="./Images/background.png" />
        </Grid.Background>

        <Rectangle Name="titlebar" Grid.Row="0" Grid.ColumnSpan="3" Height="35" VerticalAlignment="Top" Fill="Black" MouseLeftButtonDown="titlebar_MouseLeftButtonDown"/>
        <Button Grid.Row="0" Grid.Column="2" Content="X" Height="26" HorizontalAlignment="Right" Margin="0,5,5,0" Name="btnClose" VerticalAlignment="Top" Width="28"/>
		
        <StackPanel Grid.Row="1" Margin="0,-2,0,0">
            <Label Content="ID" Height="30" HorizontalAlignment="Right" Name="lbID" VerticalAlignment="Center" FontWeight="Bold" Foreground="White" FontSize="15"/>
            <Label Content="PW" Height="30" HorizontalAlignment="Right" Name="lbPW" VerticalAlignment="Center" FontWeight="Bold" Foreground="White" FontSize="15"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="1" HorizontalAlignment="Center">
            <TextBox Height="26" Width="200" HorizontalAlignment="Left" Background="#595959" Foreground="LightGray"  Name="txtID" Margin="0,0,0,5" BorderBrush="Gray" />
            <PasswordBox Height="26" Width="200" HorizontalAlignment="Left" Background="#595959" Foreground="LightGray" Name="txtPW" BorderBrush="Gray" />
        </StackPanel>

        <Button HorizontalAlignment="Left" Grid.Row="1" Grid.Column="2"  Name="btnLogin" VerticalAlignment="Top" Background ="White" Style="{StaticResource NullBackgroundButtonStyle}" Margin ="-6,-5,0,0">
            <Image Source="./Images/login.png" Stretch="None" />
        </Button>
        <ScrollViewer Name ="txtStatusBoxScroll" Grid.Row="1" Grid.RowSpan="2"  Grid.ColumnSpan="3" VerticalScrollBarVisibility="Auto" Margin="70, 110, 40, 20">            
            <TextBlock Name="txtStatusBox" Foreground="DarkGray"/>
        </ScrollViewer>
    </Grid>
</Window>

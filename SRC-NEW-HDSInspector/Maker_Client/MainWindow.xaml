<Window x:Class="Maker_Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Button Name="btnConnect">Connect</Button>
        <ScrollViewer HorizontalScrollBarVisibility="Hidden" Name="svLogViewer" VerticalScrollBarVisibility="Auto" Grid.Row="1"
                      HorizontalAlignment="Stretch">
            <ListBox SelectionMode="Single" Name="lbLog" Background="Transparent">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Path=Time, StringFormat='MM/dd hh:mm tt'}" Width="110" Margin="5, 0, 0, 0"/>
                            <TextBlock Text="{Binding Path=Message}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>
    </Grid>
</Window>
